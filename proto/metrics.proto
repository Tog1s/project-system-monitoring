syntax = "proto3";
package metrics;
option go_package = "./;pb";

service Metrics {
    rpc Get(Request) returns (Response) { 
    }
} 

message Request {
    string query = 1;
}

message Response {
    float OneMinutes = 1;
}

